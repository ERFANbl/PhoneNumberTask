
## 📌 معرفی پروژه

این پروژه شامل دو بخش است:

1. **Backend (ASP.NET Core Web API)**
   مسئول دریافت شماره تلفن و ذخیره آن در دیتابیس (SQL Server).

2. **Frontend (Static HTML/CSS/JS)**
   یک صفحه ساده جهت وارد کردن شماره تلفن و ارسال آن به API.

---

# 🚀 نحوه اجرای پروژه

## 1️⃣ اجرای Backend (Web API)

### پیش‌نیازها:

*  .NET 8
* SQL Server
* Visual Studio 

### مراحل اجرا:

1. پروژه را در Visual Studio باز کنید (`.sln`).
2. از فایل `appsettings.json` مطمئن شوید ConnectionString صحیح به SQL Server دارد.
3. پروژه را **Run (F5)** کنید.

در کنسول باید ببینید:

```
Now listening on: http://localhost:5500
```

این یعنی API روی پورت **5500** اجرا شده و مسیر POST این است:

```
POST http://localhost:5500/api/UserPhoneNumber/SavePhoneNumber
```

---

## 2️⃣ اجرای Frontend

### پیش‌نیازها:

* Node.js
* بسته‌ی serve

### نصب serve (اگر نصب نیست):

```bash
npm install -g serve
```

### اجرای فرانت:

داخل پوشه‌ای که فایل HTML در آن قرار دارد اجرا کنید:

```bash
serve -l 5000
```

سپس صفحه در این آدرس در دسترس است:

```
http://localhost:5000
```

فرانت‌اند از طریق JavaScript درخواست POST به API ارسال می‌کند:

```
http://localhost:5500/api/UserPhoneNumber/SavePhoneNumber
```

---
